{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run",
            "type": "shell",
            "command": "g++ -ggdb -Wall main.cpp -o main.exe | ./main.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}